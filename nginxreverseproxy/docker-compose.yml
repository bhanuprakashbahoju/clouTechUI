version: "3.8"
services:
  nginx:
    image: nginx:latest
    restart: unless-stopped
    ports:
      - 80
      - 443
    volumes:
      # mount the configuration file from this repo into the container
      - ./nginx.conf:/etc/nginx/nginx.conf

# no other files needed for now; the nginx.conf lives beside this compose file
